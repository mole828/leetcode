{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Kotlin: Debug current file",
      "type": "java",
      "request": "attach",
      "hostName": "localhost",
      "port": 5005,
      "preLaunchTask": "Kotlin: Start debug server for current file",
      "sourcePaths": [
        "${workspaceFolder}"
      ]
    }
  ]
}
